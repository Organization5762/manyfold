# RFC Implementation Checklist

Generated from `docs/rfc/wiregraph_rfc_rev2.md` by `manyfold-rfc-checklist`.

## Core Sections
- [x] 6. Core vocabulary and object model (Typed identity objects, routes, ports, and producer/runtime refs are in the scaffold.)
- [x] 7. Descriptor model (Descriptors now derive route-specific policies for identity, time, flow, retention, visibility, and debug behavior.)
- [x] 8. Envelope, metadata, and payload model (Closed envelopes stay metadata-only while payload bytes live in a separate store and are joined on demand with route audit events.)
- [x] 9. Read and write namespace model (Route and write-binding validation now enforce read/write plane semantics and write.shadow coherence contracts.)
- [x] 10. Time model, taints, and scheduling guards (Writes now carry clock-domain metadata plus determinism/coherence/scheduling taints, and control loops emit epoch guards.)
- [x] 11. Write model, feedback loops, and shadow semantics (WriteBinding writes now update desired shadow state and expose write-binding coherence through the runtime.)
- [x] 12. Execution model (The in-memory runtime now routes emitted envelopes across explicit mailbox boundaries and control-loop epochs.)
- [x] 13. Backpressure and credit-based flow control (Mailbox capacity, overflow handling, and credit snapshots provide a minimal credit-based backpressure surface.)
- [x] 14. Stateful operators, windows, and joins (The Python graph now exposes first-class stateful_map, window, join_latest, and materialize helpers.)
- [x] 15. Cross-partition joins (Join planning now distinguishes local, repartition, broadcast-mirror, and lookup joins with explicit visible repartition nodes.)
- [x] 16. Middleware (Middleware is now a first-class registration surface with RFC-aligned preservation checks.)
- [x] 17. Transport and link model (Transport links now expose typed capability declarations that mesh bindings validate against.)
- [x] 18. Mesh primitives (Bridge-style mesh primitives now register as explicit graph topology instead of hidden wiring.)
- [x] 19. Query plane and debug plane (Query request/response routes and well-known debug event routes are now modeled as streams.)
- [x] 20. Third-party consumption and security (Route export visibility and per-principal capabilities now gate metadata, payload, replay, debug, and validation access.)
- [x] 21. Embedded device profile (Embedded firmware profile helpers and embedded-rule validation now cover scalar and bulk device routes.)
- [x] 22. API design constraints (The public API already prefers typed refs over ad hoc string identities.)
- [x] 23. Reference example suite (The repository now ships a named RFC reference example suite, with runnable coverage for the currently supported examples.)
- [x] 24. Recommended Python-facing SDK shape (Python wrapper ergonomics now include Rx-style observe/publish composition helpers.)

## Appendix F Acceptance Criteria
- [x] Common framework for flashed devices over serial, BLE, radio, USB, shared memory, and IP links (Link registration now covers the RFC transport families as typed capability-bearing scaffolds.)
- [x] Raw and logical stream handling (Raw/logical route layers are modeled in the current API.)
- [x] Real-time debugging and coherent flow exposure (Query request/response and debug event routes now surface coherent runtime flow as streams.)
- [x] Retry, filtering, backpressure, overflow, and rate matching (Mailbox overflow handling and credit snapshots now make backpressure and rate matching observable in the runtime.)
- [x] Windows, aggregations, and streaming joins (Windowing, stateful map, materialization, and latest-value joins are available from the Python API.)
- [x] Middleware as a first-class composition surface (Middleware now registers explicitly with RFC preservation constraints.)
- [x] Transport-flexible mesh building blocks (Links and mesh primitives now compose explicitly in the graph surface.)
- [x] Explicit support for write-back loops and shadow semantics (WriteBinding request writes now project into desired shadow state through the runtime.)
- [x] Randomness and determinism explicitly modeled (Determinism taints are now attached to ephemeral/non-replayable writes as part of the time model.)
- [x] Scheduling and out-of-order bugs made harder to express (Control-loop epoch writes and explicit mailbox boundaries now make scheduling edges visible in the runtime.)
- [x] Metadata/payload split with lazy payload opening (Payload bytes now live outside ClosedEnvelope metadata and are opened lazily from the payload store.)
- [x] Query plane modeled as streams (Query request/response routes now behave like normal streams.)
- [x] No ad hoc strings in the typed runtime API (Typed refs are already the primary runtime surface.)
- [x] Protobuf wire schema appendix (The extracted schema scaffold is in the repository.)
- [x] Glossary, examples, appendices, and normative language (The RFC document already includes these sections.)
